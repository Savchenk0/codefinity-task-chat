{
	"name": "codefinity-task-chat",
	"version": "1.0.0",
	"description": "Simple and nice chat application",
	"main": "index.js",
	"scripts": {
		"install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
		"start": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\""
	},
	"author": "Savchenko Oleksandr",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^8.0.0"
	}
}
